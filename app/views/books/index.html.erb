<%= render 'list', books: @books %>

<h1>Bookers</h1>

<% books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
    <%= image_tag book.user.get_profile_image(100,100) %>
  <% end %>
  <%= book.title %>
  <%= book.body %>
</div>
<% end %>